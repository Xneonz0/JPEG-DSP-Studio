# Core dependencies
PySide6>=6.5.0
numpy>=1.24.0
scipy>=1.11.0
opencv-python>=4.8.0
scikit-image>=0.21.0
matplotlib>=3.7.0

# Testing
pytest>=7.4.0

# Note: PDF report generation uses matplotlib.backends.backend_pdf (included with matplotlib)
# reportlab is NOT required

# =============================================================================
# Optional GPU Enhancement Dependencies
# =============================================================================
# The Enhancement tab supports GPU-accelerated super-resolution with Real-ESRGAN.
# These dependencies are OPTIONAL - the app works without them using CPU Lanczos.
#
# To enable GPU enhancement, install PyTorch with CUDA support first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Then install Real-ESRGAN:
#   pip install realesrgan basicsr
#
# Requirements (if installing manually):
# torch>=2.0.0           # PyTorch with CUDA support
# realesrgan>=0.3.0      # Real-ESRGAN inference
# basicsr>=1.4.2         # Required by realesrgan
# =============================================================================

